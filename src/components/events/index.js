import useDocumentTitle from "../../hooks/useDocumentTitle";

function Events() {
  useDocumentTitle("Events in LOTR: Rise to War");
  return (
    <section>
      <h1>Events</h1>
    </section>
  );
}

export default Events;
